To install these tmux configurations simply symlink the files to your home:

   cd tmux
   ln -s ./.tmux.conf ~/

Or simply execute the install-sh from within the tmux/ directory:

   cd tmux
   ./install-sh